{
  "mcpServers": {
    "chittyos": {
      "command": "/bin/sh",
      "args": ["-lc", "TOKEN=\"$(/Users/nb/.local/bin/chitty-mcp-token chittymcp 2>/dev/null || true)\"; if [ -n \"$TOKEN\" ]; then export MCP_AUTH_HEADER=\"Bearer $TOKEN\"; fi; exec npx -y mcp-remote https://mcp.chitty.cc/mcp"],
      "env": {}
    }
  }
}
